# make a Turtle class with...
# PROPERTIES
#   coordinates = [x coordinate integer, y coordinate integer]
#   color = color string
#   thickness = turtle thickness
# FUNCTIONS
#   move(x coordinate, y coordinate) = move the turtle, update the coordinates diciontary
#   change_color(color) = update the color
#   change_thickness(thickness) = update the thickness
# then create an instance of that class and test the methods!
